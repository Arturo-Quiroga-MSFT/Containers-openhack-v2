apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "aqakscluster-2587"
    labels:
        app: "aqakscluster-2587"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: hack-web-aqakscluster-2587.4a680e702b4f4f89ac6e.canadacentral.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "aqakscluster-2587"
                servicePort: 88