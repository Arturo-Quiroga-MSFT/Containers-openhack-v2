apiVersion: v1
kind: Service
metadata:
    name: "user-java"
    labels:
        app: "user-java"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "user-java"