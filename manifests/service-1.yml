apiVersion: v1
kind: Service
metadata:
    name: "aqakscluster-84bd"
    labels:
        app: "aqakscluster-84bd"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aqakscluster-84bd"