apiVersion: v1
kind: Service
metadata:
    name: "tripviewer"
    labels:
        app: "tripviewer"
spec:
    type: ClusterIP
    ports:
    - port: 88
      targetPort: 88
      protocol: TCP
      name: http
    selector:
        app: "tripviewer"