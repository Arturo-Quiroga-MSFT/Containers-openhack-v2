apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "user-profile"
    labels:
        app: "user-profile"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: hackapi-aqakscluster-9d9b.4a680e702b4f4f89ac6e.canadacentral.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "user-profile"
                servicePort: 82